import React from "react";
import { Text, StyleSheet } from "react-native";

const Flash = () => {
  return <Text style={styles.text}>Flash Cards Screen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default Flash;
